#──────────────────────────────────────────────────────────────────────────────
# Minimal Anime-Themed Hyprlock Configuration (Fun Edition)
#──────────────────────────────────────────────────────────────────────────────

#── Global background (no blur/dark by default, subtle vignette) ─────────────
background {
    monitor =
    path = /home/saiatama1784/Pictures/wallpapers/wallhaven-9d3l21.png
    color = rgba(0,0,0,0)            # wallpaper shows through
    blur_passes = 0                  # no blur until input is active
    blur_size   = 0
    contrast    = 1.0
    brightness  = 1.0
    vibrancy    = 0.0
    vibrancy_darkness = 0.0
    overlay_color = rgba(0,0,0,0.10) # very gentle vignette
}

#── Password input field (activates blur, darken & animations) ────────────────
input-field {
    monitor =
    size = 240, 52                   # slightly larger
    outline_thickness = 3
    # pulsing gradient in your wallpaper’s purples→teals
    outline_gradient = radial(rgba(180,100,200,0.6), rgba(100,200,220,0.6))
    outline_pulse = true             # gently pulses the gradient
    dots_char = "★"                  # star-shaped dots!
    dots_size    = 0.28
    dots_spacing = 0.8
    dots_center  = true
    dots_color_animated = true       # cycles through gradient colors
    outer_color = rgba(0,0,0,0.6)    # dark semi‑transparent backdrop
    inner_color = rgba(0,0,0,0.8)    # deeper dark when active
    font_color  = rgba(240,240,255,0.9)
    placeholder_text = "🔒 Password"  # padlock icon
    hide_input   = true
    fade_on_empty = true
    fade_timeout = 600               # hide quickly after typing
    background_blur = true           # enable blur when focused
    background_blur_passes = 2       # stronger blur
    background_blur_size   = 6
    background_overlay_color = rgba(0,0,0,0.4) 
                                      # darken wallpaper behind input
    position = 0, 20                 # center + down a bit
    halign = center
    valign = center
    check_color    = rgba(200,120,180,0.7)
    capslock_color = rgba(250,180,180,0.7)
    visible_only_on_keyboard = true  # only show on typing
}

# #── Clock (hidden until typing) ────────────────────────────────────────────────
# label {
#     monitor =
#     text = $TIME
#     font_family = "Roboto Mono"
#     font_size   = 44
#     color       = rgba(240,240,255,0.8)
#     position = 0, -70               # above the input field
#     halign = center
#     valign = center
#     visible_only_on_keyboard = true
#     visible_timeout = 600
# }

# #── Date (now also hidden until typing) ────────────────────────────────────────
# label {
#     monitor =
#     text = $DATE
#     font_family = "Roboto Mono"
#     font_size   = 18
#     color       = rgba(240,240,255,0.6)
#     position = 0, -110              # sits above the clock
#     halign = center
#     valign = center
#     visible_only_on_keyboard = true
#     visible_timeout = 600
# }

#── Fun hint (fades out when you start typing) ────────────────────────────────
# label {
#     monitor =
#     text = "✨ Tap any key to unleash the magic ✨"
#     font_family = "JetBrains Mono"
#     font_size   = 13
#     color       = rgba(220,220,255,0.4)
#     position = 0, 90                # below the input field
#     halign = center
#     valign = center
#     fade_timeout = 500
# }
